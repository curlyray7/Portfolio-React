import "./HeroButtons.css"

const HeroButtons = () => {
    return (
        <div className="buttons">
            <button className="linkedin_button" onClick={() => {}}>LinkedIn</button>
            <button className="projects_button" onClick={() => {}}>Projects</button>
        </div>
    )
}

export default HeroButtons;